#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main()
{
        char myChar[] = "\"Programming is like building a multilingual puzzle";
	puts(myChar);
	return(0);
}
